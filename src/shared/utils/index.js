/**
 * BackStage — Fonctions utilitaires partagées
 *
 * import { parseDate, fmtDate, intOnly, maskIban } from '../shared/utils'
 */

export {
  parseDate,
  fmtDate,
  fmtDateShort,
  intOnly,
  decimalOnly,
  maskIban,
  maskSS,
  fmtCurrency,
} from './format'
